{% extends 'base.html.twig' %}

{% block title %}Login!{% endblock %}

{% block body %}

{# <form action="{{ path('app_login') }}" method="POST">
<div class="d-flex flex-column align-items-center justify-content-center bg-secondary">
<div>
    <lable class="form-label">Email</label>
    <input type="text" name="_username" value="{{ lastUserName }}" class="form-control"/>
</div>
<div>
    <lable class="form-label">Password</label>
    <input type="password" name="_password" class="form-control"/>
</div>
<input type="hidden"  name="_target_path" value="/" />
<div>
  <button type="submit" class="btn btn-primary w-100 mt-2">Login!</button>
</div>
</div>
</form> #}

<div class="container my-5 bg-secondary p-5 rounded-3">
<div class="container text-center mb-5">
<h1 class="text-white fs-2">E-store Admin Panel</h1>
<h2 class="text-white fs-4">Login Panel</h2>
</div>
{% if error %}
        <div class="alert alert-danger">
            {{ error.messageKey|trans(error.messageData, 'security') }}
        </div>
    {% endif %}
  <form action="{{ path('app_login') }}" method="POST">
    <div class="mb-3">
      <label for="email" class="form-label">Email address</label>
      <input type="text" name="_username" value="{{ lastUserName }}" class="form-control" />
    </div>
    <div class="mb-3">
      <label for="password" class="form-label">Password</label>
      <input type="password" name="_password" class="form-control" />
    </div>

<input type="hidden"  name="_target_path" value="/" />
<div class="">
    <button type="submit" class="btn btn-primary w-100">Sign in</button>
    </div>
  </form>
  <div class="mt-3">
  <h3 class="text-white fs-4">Example admin account</h3>
   <h4 class="text-white fs-5">Login</h4>
   <h5 class="text-white fs-6">admin@test.pl</h5>
   <h4 class="text-white fs-5">Password</h4>
   <h5 class="text-white fs-6">12345678</h5>
  </div>
</div>


{{ include('/admin_panel/_footer.html.twig') }}
{% endblock %}
