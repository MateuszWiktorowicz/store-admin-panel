{% extends 'base.html.twig' %}

{% block body %}
<main>

<section class="d-flex flex-column flex-sm-row-reverse p-3">
<div class="col-12 col-md-10 d-flex flex-column p-4 bg-section rounded-end-3">
<div class="px-5">{{ include('/admin_panel/_header.html.twig') }}</div>
<div class="bg-secondary p-3 rounded-3">{% block content %}{% endblock %}</div>
</div>
<div class="col-2 d-none d-md-block">{{ include('/admin_panel/_side_menu.html.twig') }}</div>

</section>
<div class="">{{ include('/admin_panel/_footer.html.twig') }}</div>
</main>



{% endblock %}
